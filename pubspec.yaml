name: yuetu
description: 月兔 - 记账 / 任务 / 笔记 / 专注 / 打卡
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2
  flutter_quill: ^11.4.0
  speech_to_text: ^7.0.0
  lucide_icons_flutter: ^3.1.20
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
